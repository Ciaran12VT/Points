<?xml version="1.0" encoding="utf-8" ?>
<ContentPage
    x:Class="Points.Views.Trackers.ValueTrackerDetailsPage"
    xmlns="http://schemas.microsoft.com/dotnet/2021/maui"
    xmlns:x="http://schemas.microsoft.com/winfx/2009/xaml"
    Title="Value Tracker">

    <ScrollView>
        <VerticalStackLayout Padding="16" Spacing="14">

            <!-- Title -->
            <Grid ColumnDefinitions="*,Auto" ColumnSpacing="12" VerticalOptions="Center">
                <Label Text="Title"
                       FontAttributes="Bold"
                       FontSize="14"
                       VerticalOptions="Center" />

                <Button Grid.Column="1"
                        Text="X"
                        BackgroundColor="DarkRed"
                        TextColor="White"
                        WidthRequest="36"
                        HeightRequest="36"
                        CornerRadius="18"
                        Padding="0"
                        HorizontalOptions="End"
                        VerticalOptions="Center"
                        Command="{Binding DeleteCommand}" />
            </Grid>

            <Entry x:Name="TitleEntry"
                   Placeholder="e.g. Body Weight"
                   Text="{Binding Title}" />

            <!-- Unit -->
            <Label Text="Unit" FontAttributes="Bold" FontSize="14" />
            <Entry x:Name="UnitEntry"
                   Placeholder="e.g. kg, €, bpm"
                   Text="{Binding Unit}" />

            <!-- Start Date -->
            <Label Text="Start date" FontAttributes="Bold" FontSize="14" />
            <DatePicker x:Name="StartDatePicker"
                        Date="{Binding StartDate}"
                        HorizontalOptions="Fill" />

            <!-- Initial Values -->
            <Label Text="Initial values (optional)" FontAttributes="Bold" FontSize="14" />
            <Label Text="Comma separated (e.g. 72.4, 72.1, 71.9)"
                   FontSize="12"
                   Opacity="0.65" />
            <Editor x:Name="ValuesEditor"
                    AutoSize="TextChanges"
                    HeightRequest="90"
                    Placeholder="72.4, 72.1, 71.9"
                    Text="{Binding InitialValuesText}" />

            <!-- Schedule -->
            <!--<Label Text="Schedule (record only for now)" FontAttributes="Bold" FontSize="14" />
            <Grid ColumnDefinitions="Auto,*,Auto,Auto" ColumnSpacing="10" VerticalOptions="Center">
                <Label Text="Every" VerticalTextAlignment="Center" />

                <Entry x:Name="EveryEntry"
                       Grid.Column="1"
                       Text="1"
                       Keyboard="Numeric"
                       HorizontalTextAlignment="Center" />

                <Picker x:Name="UnitPicker"
                        Grid.Column="2"
                        WidthRequest="140" />

                <Label Grid.Column="3"
                       Text="from start date"
                       VerticalTextAlignment="Center"
                       Opacity="0.7" />
            </Grid>-->
            <Label Text="Schedule" FontAttributes="Bold" FontSize="14" />

            <Grid ColumnDefinitions="*,Auto"
                  ColumnSpacing="12"
                  VerticalOptions="Center">

                <Label Text="{Binding ScheduleSummaryText}"
                       Opacity="0.75"
                       VerticalTextAlignment="Center" />

                <Button Grid.Column="1"
                        Text="📅"
                        WidthRequest="36"
                        HeightRequest="36"
                        CornerRadius="18"
                        Padding="0"
                        HorizontalOptions="End"
                        VerticalOptions="Center"
                        BackgroundColor="Green"
                        Command="{Binding EditSchedulesCommand}" />
            </Grid>

            <Button Text="Metadata Fields"
                    TextColor="White"
                    BackgroundColor="DodgerBlue"
                    CornerRadius="12"
                    Command="{Binding EditUdmdCommand}" />

            <Label Text="Recorded Metadata"
                   FontAttributes="Bold"
                   FontSize="14"
                   IsVisible="{Binding HasMetadataHistory}" />
            <VerticalStackLayout
                                 Spacing="6"
                                 IsVisible="{Binding HasMetadataHistory}"
                                 BindableLayout.ItemsSource="{Binding MetadataHistoryRows}">
                <BindableLayout.ItemTemplate>
                    <DataTemplate>
                        <Label Text="{Binding .}"
                               FontSize="13"
                               Opacity="0.8" />
                    </DataTemplate>
                </BindableLayout.ItemTemplate>
            </VerticalStackLayout>


            <!-- Error -->
            <Label Text="{Binding ErrorText}"
                   TextColor="OrangeRed"
                   IsVisible="{Binding HasError}" />

            <!-- Buttons -->
            <Grid ColumnDefinitions="*,*" ColumnSpacing="12" Margin="0,10,0,0">
                <Button Grid.Column="0"
                        Text="Cancel"
                        BackgroundColor="Gray"
                        TextColor="White"
                        FontAttributes="Bold"
                        FontSize="21"
                        HeightRequest="48"
                        CornerRadius="12"
                        Command="{Binding CancelCommand}" />

                <Button Grid.Column="1"
                        Text="OK"
                        BackgroundColor="Green"
                        TextColor="White"
                        FontAttributes="Bold"
                        FontSize="21"
                        HeightRequest="48"
                        CornerRadius="12"
                        Command="{Binding SaveCommand}" />
            </Grid>

        </VerticalStackLayout>
    </ScrollView>
</ContentPage>
