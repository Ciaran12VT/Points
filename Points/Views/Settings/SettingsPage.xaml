<?xml version="1.0" encoding="utf-8" ?>
<ContentPage xmlns="http://schemas.microsoft.com/dotnet/2021/maui"
             xmlns:x="http://schemas.microsoft.com/winfx/2009/xaml"
             x:Class="Points.Views.Settings.SettingsPage"
             Title="Settings">

    <ScrollView>
        <VerticalStackLayout Padding="12" Spacing="12">

            <Frame Padding="0"
                   CornerRadius="12"
                   HasShadow="False"
                   BorderColor="#DDDDDD">

                <VerticalStackLayout Spacing="0">

                    <Button Text="Database"
                            Clicked="OnDatabaseSettingsClicked"
                            HorizontalOptions="Fill"
                            BackgroundColor="Transparent"
                            TextColor="{AppThemeBinding Light=Black, Dark=White}"
                            FontAttributes="Bold"
                            HeightRequest="56" />

                    <BoxView HeightRequest="1"
                             Color="#DDDDDD" />

                    <Button Text="Multipliers"
                            Clicked="OnMultipliersSettingsClicked"
                            HorizontalOptions="Fill"
                            BackgroundColor="Transparent"
                            TextColor="{AppThemeBinding Light=Black, Dark=White}"
                            FontAttributes="Bold"
                            HeightRequest="56" />

                    <BoxView HeightRequest="1"
                             Color="#DDDDDD" />

                    <Button Text="Modules &amp; Features"
                            Clicked="OnModulesAndFeaturesSettingsClicked"
                            HorizontalOptions="Fill"
                            BackgroundColor="Transparent"
                            TextColor="{AppThemeBinding Light=Black, Dark=White}"
                            FontAttributes="Bold"
                            HeightRequest="56" />

                    <BoxView HeightRequest="1"
                             Color="#DDDDDD" />

                    <Button Text="Notifications Log"
                            Clicked="OnNotificationsLogClicked"
                            HorizontalOptions="Fill"
                            BackgroundColor="Transparent"
                            TextColor="{AppThemeBinding Light=Black, Dark=White}"
                            FontAttributes="Bold"
                            HeightRequest="56" />

                </VerticalStackLayout>
            </Frame>

        </VerticalStackLayout>
    </ScrollView>
</ContentPage>
